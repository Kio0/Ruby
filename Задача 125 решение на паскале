//задача 125
{
Палиндромное число 595 интересно, потому что его можно записать в виде суммы последовательных квадратов: 6^2 + 7^2 + 8^2 + 9^2 + 10^2 + 11^2 + 12^2 .
Существует ровно одиннадцать палиндромов ниже одной тысячи, которые можно записать в виде последовательных квадратных сумм, 
и сумма этих палиндромов равна 4164. Обратите внимание, что 1 = 0^2 + 1^2 не было включено, поскольку эта проблема связана с квадратами положительных целые числа.
Найдите сумму всех чисел меньше 10^8, которые являются палиндромными и могут быть записаны как сумма последовательных квадратов.
}
const Max=100000000;
var i,r,s: int64;
function testP(n:integer): boolean; //проверяем число на палиндромность
var s: string;
var b: boolean;
var i: integer;
begin
  b:=true;
  s:=n+'';
  for i:=1 to length(s) div 2 do if s[i]<>s[length(s)-i+1] then b:=false;
  testP:=b;
end;
function sum(n1,n2: integer): int64; //считает сумму квадратов всех чисел от n1 до n2 включительно
var i,ss: int64;
begin
for i:=n1 to n2 do ss:=ss+i*i;  
sum:=ss;  
end;


var Itog: int64;
begin
  for i:=1 to round(sqrt(Max)) do  //выбираем первое число в последовательности
    for r:=i+1 to round(sqrt(Max)) do begin //выбираем второе число в последовательности
     s:=sum(i,r);
     if s>Max then break;
     if testP(s) then itog:=itog+s;
    
  end;
  writeln(itog);
end.
